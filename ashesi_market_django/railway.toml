[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate --fake-initial && python manage.py create_superuser_if_none && gunicorn ashesi_market.wsgi --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
